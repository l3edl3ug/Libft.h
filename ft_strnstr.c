/* ************************************************************************** */
/*                                                                            */
/*                                                        :::      ::::::::   */
/*   ft_strnstr.c                                       :+:      :+:    :+:   */
/*                                                    +:+ +:+         +:+     */
/*   By: tphoonsi <tphoonsi@student.42.fr>          +#+  +:+       +#+        */
/*                                                +#+#+#+#+#+   +#+           */
/*   Created: 2024/08/28 12:52:32 by tphoonsi          #+#    #+#             */
/*   Updated: 2024/09/07 15:20:14 by tphoonsi         ###   ########.fr       */
/*                                                                            */
/* ************************************************************************** */

#include "libft.h"

char	*ft_strnstr(const char *s1, const char *s2, size_t n)
{
	size_t	i;
	size_t	j;

	i = 0;
	if (!s1 && !s2)
		return ('\0');
	if (s2[0] == '\0')
		return ((char *)s1);
	while (s1[i] && i < n)
	{
		j = 0;
		while ((s1[i + j]) && s2[j] && (s1[i + j] == s2[j]) && i + j < n)
			j++;
		if (s2[j] == '\0')
			return ((char *)s1 + i);
		i++;
	}
	return (NULL);
}
/*
find needle in haystack
while (s1[i] && i < n):
ลูปจะวนไปจนกว่า s1[i] จะเป็น \0 (สิ้นสุดสตริง) หรือ i จะเกิน n 
(ขอบเขตที่กำหนด)

j = 0; while ((s1[i + j]) && s2[j] && (s1[i + j] == s2[j]) && i + j < n):
ลูปนี้เปรียบเทียบอักขระใน s1 กับ s2 ทีละตัว. ถ้าอักขระตรงกัน ลูปจะดำเนินต่อไป.
ลูปจะหยุดเมื่ออักขระไม่ตรงกัน, s2[j] เป็น \0 (หมายถึงสตริงย่อยถูกพบ)
หรือเมื่อ i + j เกิน n

if (s2[j] == '\0') return ((char *)s1 + i);:
ถ้า s2[j] เป็น \0, หมายความว่าค้นหาสตริงย่อย s2 พบใน s1
และฟังก์ชันจะคืน pointer ไปยังตำแหน่งที่พบ

สมมุติว่า:

s1 = "Hello, World!"
s2 = "World"
n = 12
ฟังก์ชันเริ่มต้นที่ i = 0 (s1[0] = 'H') เปรียบเทียบกับ s2[0] ('W') ไม่ตรงกัน
ขยับไป i = 1 (s1[1] = 'e') ไปเรื่อยๆ จนถึง i = 7 (s1[7] = 'W') ที่ตรงกับ s2[0]

ฟังก์ชันจะเริ่มวนลูปเปรียบเทียบอักขระ:

s1[7] กับ s2[0] ('W' == 'W') → ตรงกัน, j = 1
s1[8] กับ s2[1] ('o' == 'o') → ตรงกัน, j = 2
s1[9] กับ s2[2] ('r' == 'r') → ตรงกัน, j = 3
s1[10] กับ s2[3] ('l' == 'l') → ตรงกัน, j = 4
s1[11] กับ s2[4] ('d' == 'd') → ตรงกัน, j = 5
เมื่อ j = 5, s2[5] == '\0', ฟังก์ชันจะคืนค่า s1 + 7 
ซึ่งเป็นตำแหน่งเริ่มต้นที่พบสตริงย่อย "World" ใน "Hello, World!"
และถ้ากำหนด n น้อยกว่าที่จะเจอสตริงย่อย
สมมุติ n = 3, ฟังก์ชัน ft_strnstr จะตรวจสอบแค่ 3 อักขระแรกของ s1 ('H', 'e', 'l')
ซึ่งไม่เพียงพอที่จะพบ "World". ดังนั้น ฟังก์ชันจะคืนค่า NULL
#include <stdio.h>
int main()
{
	const char haystack[] = "Hello, this is a simple test.";
	const char needle[] = "this";
	char	*result;

	result = ft_strnstr(haystack, needle, 30);
	printf("Found '%s' in '%s' -> '%s'\n", needle, haystack, result);
    return 0;
}
*/
